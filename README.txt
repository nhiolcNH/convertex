English:

ONLY FOR LINUX

Maybe you would execute as a .cpp on your terminal.
Remember, is just a basic program that I made with c++ 'cause i was bored, isn't "perfect".

open on your terminal as a root: "sudo su"
to compile the program: g++ convertex.cpp
then, execute with: ./a.out

Do not execute with "./" sometimes it shows errors with the code, just compile with g++.




Español:

SOLO PARA LINUX

Tal vez debas ejecutarlo como .cpp en la terminal.
Recuerda que solo es un programa básico que hice en c++ porqué estaba aburrido, no es "perfecto".

Abrelo en tu terminal como root: "sudo su"
Para compilarlo use: "g++ convertex.cpp"
Después ejecutalo con: "./a.out"

No lo ejecutes con "./" ya que a veces muestra errors con el código, ejecutalo solo con el compilador (g++
